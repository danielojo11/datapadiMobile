--- app/(provider)/components/drawers/PrintPreview.tsx
+++ app/(provider)/components/drawers/PrintPreview.tsx
@@ -19,7 +19,7 @@
   if (!batches || batches.length === 0) return null;
 
   const allPins = batches.flatMap(batch => 
-    batch.pins.map(pin => ({
+    batch.pins.map((pin: any) => ({
       ...pin,
       networkId: batch.networkId,
       amount: batch.amount,
--- app/(provider)/components/drawers/PinBatchModal.tsx
+++ app/(provider)/components/drawers/PinBatchModal.tsx
@@ -112,7 +112,7 @@
 
             {/* Vouchers List */}
             <View style={styles.vouchersList}>
-              {batch.pins.map((pin, idx) => (
+              {batch.pins.map((pin: any, idx: number) => (
                 <TouchableOpacity
                   key={idx}
                   activeOpacity={0.7}
